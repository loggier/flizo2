
"use client";

// This component is currently not used for active push registration logic,
// as that has been moved to the login flow for better timing.
// It can be used in the future for handling incoming notifications
// while the app is active, or for other background tasks.

const PushNotificationHandler = () => {
    return null; 
};

export default PushNotificationHandler;
